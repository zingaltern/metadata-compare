# 元数据自动化比对系统（一期 MVP）— 详细技术文档

> 项目路径：`/Users/zingaltern/WorkBuddy/2026-07-27-21-43-25/metadata-compare`
> 技术基线：Spring Boot 3.2.12 / Java 17
> 文档版本：与当前源码（已端到端验证通过）一致

---

## 1. 项目概述

本系统用于**跨数据源的元数据一致性自动比对**。它把来自多个源头（生产库 DDL、DDM 模型、SOA 接口、文件规范）的表/字段元数据，统一解析成中间格式，再按「表-字段」关联，用可配置的规则引擎逐条判定，发现不一致与违规，对严重问题自动生成人工复核工单，并支持定时调度与邮件通知。

**一期 MVP 目标（已达成）**
- 统一解析 4 类数据源 → 统一中间格式
- 规则引擎（QLExpress）可配置、可热加载
- 分级结果（CRITICAL / WARNING / INFO）+ 自动复核工单
- 定时调度（cron 驱动）
- 邮件通知（SMTP 未配置时优雅降级）
- H2 开箱即跑；MySQL 生产 profile 一键切换

**典型使用场景**
- 上线前自动核对「生产表结构 ↔ 设计模型 ↔ 接口字段 ↔ 落地下发文件规范」是否一致
- 命名规范审计（禁止中文字段名、增量表必须有时间戳等）
- 把比对结果推给相关人复核、留痕、跟踪整改

---

## 2. 技术栈

| 维度 | 选型 | 说明 |
|---|---|---|
| 框架 | Spring Boot 3.2.12 / Java 17 | 主框架 |
| 规则引擎 | QLExpress 3.3.4（阿里） | 注意：Maven Central 最大版本即 3.3.4，并非方案里写的 4.1.2（公开仓库无此版本）；`ExpressRunner`/`addFunction` API 一致 |
| SQL 解析 | JSqlParser 4.9 | `CreateTable` 类（4.x 把 `Create` 改名） |
| OpenAPI 解析 | swagger-parser 2.1.22 | 解析 OpenAPI 3 / Swagger 2 |
| Excel 解析 | Apache POI 5.2.5 | `WorkbookFactory` |
| XML/JSON | Jackson `jackson-dataformat-xml` / `-yaml` | 模型与 YAML 解析 |
| ORM | MyBatis-Plus 3.5.5（`mybatis-plus-spring-boot3-starter`） | **必须用 spring-boot3-starter**，否则 SB3 下报 `factoryBeanObjectType` 错误 |
| 数据库 | H2（MySQL 兼容模式）/ MySQL 8.0 | 默认 H2，生产切 profile |
| 日志 | Log4j2 | 已排除所有 starter 的 Logback |
| 模板 | Freemarker | 邮件正文 |
| 工具 | Lombok 1.18.32 | 省略样板代码 |

---

## 3. 系统架构与数据流

```
                 data/input/
   ┌─────────────┬────────────┬────────────┬─────────────┐
   │ production  │    ddm     │    soa     │  file_spec  │
   │  (DDL)      │ (XML/JSON) │(Swagger/   │   (xlsx)    │
   │             │            │  OpenAPI)  │             │
   └──────┬──────┴─────┬──────┴─────┬──────┴──────┬──────┘
          │            │            │            │
          ▼            ▼            ▼            ▼
     ┌────────────────────────────────────────────────┐
     │  ParserRouter（SPI 有序路由，递归解析目录）        │
     └───────────────────────┬────────────────────────┘
                              │  所有源统一为
                              ▼
                   StandardMetadata（统一中间格式）
                              │
                              ▼
     ┌────────────────────────────────────────────────┐
     │  ComparePipeline：按归一化表名关联匹配            │
     │   → 拆「字段级规则」/「表级规则」                 │
     │   → 逐条构建上下文变量 → QLExpress 求值           │
     └───────────────────────┬────────────────────────┘
                              │  命中
              ┌───────────────┼────────────────┐
              ▼               ▼                ▼
        CompareResult    ReviewTicket    通知(Mail)
        （明细入库）    （CRITICAL 建单）  （SMTP 可降级）
              │               │
              ▼               ▼
          compare_task    compare_task_config
          compare_result  review_ticket
          operation_log
                              │
                              ▼
                 CompareScheduler（30s 轮询，cron 判定）
```

**关键设计点**
- **统一中间格式**：四类数据源解析后都变成 `StandardMetadata`（表 + 字段列表 + 扩展属性），规则只面对统一模型，与数据源解耦。
- **解析与规则解耦**：新增数据源只需新增一个 `MetadataParser` 实现并注册到 `ParserRouter`，不动规则引擎。
- **规则与代码解耦**：规则写在 `compare-rules.yml`，运行时热加载，无需重新编译。

---

## 4. 目录结构

```
metadata-compare/
├── pom.xml                          # 构建（SB3.2.12 parent，依赖见 §2）
├── data/input/                      # 样例输入（按文档 2.4 规范分层）
│   ├── production/latest/customer_ddl.sql
│   ├── ddm/customer_model.xml
│   ├── ddm/product_model.json
│   ├── soa/order_api.json           # OpenAPI 3
│   ├── soa/customer_api.yaml        # Swagger 2
│   └── file_spec/file_spec.xlsx
├── rules/compare-rules.yml          # 运行时规则副本（热加载目标）
├── rules/table-mappings.yml          # 表名对照表（热加载，跨源异构命名归一）
├── src/main/java/com/dqc/compare/
│   ├── MetadataCompareApplication.java
│   ├── config/                      # AppProperties / DataInitializer
│   ├── model/                       # StandardMetadata / FieldMeta / 枚举
│   ├── parser/                      # 解析接口 + 路由 + 5 个实现
│   ├── rule/                        # RuleDef / RuleEngine / RuleLoader / 自定义函数
│   ├── service/                     # ComparePipeline / CompareQueryService
│   ├── ticket/                      # ReviewTicketService
│   ├── notify/                      # MailService
│   ├── scheduler/                   # CompareScheduler
│   ├── api/rest/                    # 3 个 REST 控制器 + 异常处理器
│   ├── dto/                         # 请求/响应 DTO
│   ├── entity/                      # 6 张表实体（含 app_lock 分布式锁表）
│   └── mapper/                      # MyBatis-Plus Mapper
└── src/main/resources/
    ├── application.yml              # 默认 H2 配置 + 业务配置
    ├── application-mysql.yml        # 生产 MySQL profile
    ├── db/schema.sql                # 建表脚本（跨库兼容）
    ├── rules/compare-rules.yml      # classpath 内置规则（首次拷贝到 rules/）
    ├── log4j2.xml
    ├── static/index.html            # 浏览器落地页（触发按钮）
    └── templates/ticket-notify.ftl, run-summary.ftl
```

---

## 5. 核心数据模型

### 5.1 统一中间格式 `StandardMetadata`
| 字段 | 类型 | 说明 |
|---|---|---|
| sourceType | `SourceType` | PRODUCTION_DDL / DDM_MODEL / SOA_API / FILE_SPEC |
| tableName | String | 表名 / 实体名 / 接口名 |
| fields | `List<FieldMeta>` | 字段列表 |
| extra | `Map<String,Object>` | 扩展属性（如 `deliveryType` 下发方式） |

- `findField(name)`：按字段名忽略大小写查找
- `getDeliveryType()/setDeliveryType()`：便捷访问 `extra["deliveryType"]`（FULL / INCREMENTAL）

### 5.2 字段 `FieldMeta`
`fieldName, dataType, length, precision, nullable, comment, constraint`

### 5.3 枚举
- `Severity`：CRITICAL / WARNING / INFO
- `Category`：VIOLATION（违规）/ STRUCTURE（结构）/ LOGIC（逻辑）
- `RuleAction`：REPORT_ONLY / CREATE_REVIEW_TICKET
- `SourceType`：见上

---

## 6. 元数据解析

### 6.1 解析契约
- `MetadataParser` 接口：`parse(ParseRequest) -> List<StandardMetadata>`
- `ParseRequest` 携带文件 `Path` + 前 1KB 片段（用于嗅探格式）
- `ParserRouter` 维护**有序**解析器列表，对目录用 `Files.walk` 递归；按扩展名/内容嗅探选择解析器

### 6.2 五个解析器
| 解析器 | 输入 | 技术 | 产出要点 |
|---|---|---|---|
| `MySqlDdlParser` | `*.sql` | JSqlParser `CreateTable` | 表、字段、类型、长度、精度、可空、PK |
| `XmlModelParser` | `*.xml` | Jackson XML | DDM 模型 XML → `DdmModel` → `StandardMetadata` |
| `JsonModelParser` | `*.json` | Jackson | 普通模型 JSON；嗅探到 Swagger/OpenAPI 则转交 `SwaggerApiParser` |
| `SwaggerApiParser` | `*.json`/`*.yaml` | swagger-parser | OpenAPI 3 / Swagger 2，读取 `components.schemas`，支持 `x-deliveryType` 扩展 |
| `ExcelSpecParser` | `*.xlsx` | POI | 文件规范表，中文表头别名映射（见 §6.3） |

### 6.3 Excel 表头别名映射（重要）
`ExcelSpecParser.alias()` 把中文表头映射到标准化列。**优先级很关键**：
- `下发 / 增量 / 全量 / 交付` → **先**判为 `deliveryType`
- `类型 / 数据类型` → 判为 `dataType`（**必须放在 deliveryType 之后**，否则「下发**类型**」会被误判为 dataType，覆盖 deliveryType 列）

标准化列：`tableName / fieldName / dataType / length / precision / nullable / comment / constraint / deliveryType`

### 6.4 样例数据
| 文件 | 内容 | 预期触发 |
|---|---|---|
| `production/latest/customer_ddl.sql` | CUSTOMER（含中文 `客户名`）、ORDER、PRODUCT | 中文字符检测、`客户名` 相关缺失/可空 |
| `ddm/customer_model.xml` | 客户模型（英文 `CUST_NAME`） | 与 prod 的 `客户名` 不匹配 |
| `ddm/product_model.json` | 产品模型 | 与 prod PRODUCT 比对 |
| `soa/order_api.json` | OpenAPI 3，含 `x-deliveryType: FULL` | SOA 侧字段参与匹配 |
| `soa/customer_api.yaml` | Swagger 2 | SOA 侧字段参与匹配 |
| `file_spec/file_spec.xlsx` | AUDIT_LOG（INCREMENTAL，无时间戳）+ CONFIG（FULL，有 UPDATE_TIME） | 增量文件缺少时间戳（AUDIT_LOG） |
| `production/latest/ACCOUNT_DDL.sql` | 账户表，生产定名 `T_ACCT`（老系统无法改） | 与 DDM `ACCT_MODEL`、SOA `ACCT_INFO` 经对照表归一到 `ACCOUNT` |
| `ddm/account_model.json` | `ACCT_MODEL`，**故意缺 `CREATE_TIME` 字段** | 验证异名表关联后字段比对仍正常（命中 1 条真实缺失） |
| `soa/account_api.yaml` | Swagger 2，`ACCT_INFO`，`x-deliveryType: FULL` | 同上，参与异名归并 |

---

## 7. 规则引擎

### 7.1 运行方式
- `RuleEngine` 持有 `ExpressRunner(true, false)`（精确模式，关闭 trace 语法树日志），启动时注册 5 个自定义函数
- 每条规则单线程执行，带**软超时**（`future.get(timeoutMs)`，超时被 `cancel(true)` 中断；默认 3000ms，见 `app.rule.timeout-ms`）
- 线程池使用 `AbortPolicy`：池满时由调用线程内联执行（放弃软超时，避免中断请求线程）；QLExpress 不可中断，超时线程会继续跑完
- 规则定义：`RuleDef(name, severity, category, action, condition)`

### 7.2 自定义函数（已注册）
| 函数 | 签名 | 用途 |
|---|---|---|
| `hasChinese` | `hasChinese(s)` | 是否含中文字符（**基于码点判断，无转义陷阱**，推荐用于中文名检测） |
| `regexMatch` | `regexMatch(s, regex)` | 正则匹配（注意 QLExpress 会吞反斜杠，正则内不要写 `\` 转义） |
| `typeCompatible` | `typeCompatible(a, b)` | 类型兼容性（归一化后比较） |
| `hasTimestampField` | `hasTimestampField(fields)` | 字段列表是否含时间戳字段（UPDATE_TIME / CREATE_TIME / MODIFY_TIME 等） |
| `deliveryMismatch` | `deliveryMismatch(a, b)` | SOA 与文件规范两侧下发方式声明是否不一致（归一化后比较） |

### 7.3 规则列表（`rules/compare-rules.yml`，共 9 条）
| # | 规则名 | 级别 | 动作 | 条件（简化） |
|---|---|---|---|---|
| 1 | 中文字符检测 | CRITICAL | 建工单 | `hasChinese(field.fieldName)` |
| 2 | 字段长度不一致 | WARNING | 仅记录 | prod 与 model 长度均非空且不等 |
| 3 | 字段精度不一致 | WARNING | 仅记录 | prod 与 model 精度均非空且不等 |
| 4 | 字段类型不兼容 | WARNING | 仅记录 | `!typeCompatible(prod, model)` |
| 5 | 模型缺失生产字段 | WARNING | 仅记录 | `prodField != null && modelField == null` |
| 6 | 生产缺失模型字段 | WARNING | 仅记录 | `modelField != null && prodField == null` |
| 7 | 必填字段在模型中变为可空 | WARNING | 仅记录 | prod 非空、model 可空 |
| 8 | 增量文件缺少时间戳 | CRITICAL | 建工单 | `deliveryType == 'INCREMENTAL' && !hasTimestampField(fields)` |
| 9 | 下发方式不一致 | WARNING | 仅记录 | SOA `x-deliveryType` 与文件规范下发方式两源交叉校验不一致 |

> 规则消息不再按规则名在 Java 中硬编码：每条规则可在 YAML 声明 `message` 模板（占位符 `${tableName}`、`${fieldName}`、`${prodLength}`、`${chineseFieldName}`、`${soaDeliveryType}` 等），未配置时用通用文案；`fieldNameFromSource: true` 时结果字段名取来源侧真实名（如中文字段名）。

### 7.4 热加载
- 首次启动：`RuleLoader` 把 classpath 内置规则拷贝到 `./rules/compare-rules.yml`（若文件不存在）
- 之后每 `app.rules.hot-reload-seconds`（默认 60 秒）检测文件变更并重载
- **改规则无需重启**，改完最多等一个检测周期即生效

### 7.5 ⚠️ 重要坑：QLExpress 会吞掉字符串里的反斜杠
原规则写成 `regexMatch(field.fieldName, '[\u4e00-\u9fa5]')` 时，QLExpress 词法会把 `\u4e00` 的反斜杠吃掉，正则实际变成 `[u4e00-u9fa5]`，其中 `0-u` 被 Java 正则理解为码点区间 `0x30~0x75`，**匹配所有数字和拉丁字母 → 每条英文字段名都误报为「中文字符」**。
**结论**：规则里不要靠 `\uXXXX` 转义做中文匹配，直接用 `hasChinese()`。该问题已修复并验证。

---

## 8. 比对编排流水线（`ComparePipeline`）

**流程**
1. 解析四类数据源 → 各自按归一化表名建索引（`normalizeTable` 去掉反引号、转大写）
2. 收集所有表名（`LinkedHashSet` 保序），逐表关联 prod/ddm/soa/spec
3. 规则分两类：
   - **表级规则**（条件里不含 `field/prodField/modelField/soaField/specField`）：每表评估一次，上下文含 `tableName`、`fileSpec`、`soa`
   - **字段级规则**（含上述字段变量）：按唯一字段名逐一评估，上下文含 `field`(首选非空)、`prodField`、`modelField`、`soaField`、`specField`、`fileSpec`
4. 命中即构造 `CompareResult` 入库；CRITICAL 且动作为 `CREATE_REVIEW_TICKET` 时调用 `ReviewTicketService.createAndNotify` 建工单
5. 执行汇总邮件（仅当配置了 `recipients`）
6. 更新 `compare_task_config.last_run_time`

**字段归一化**：字段名同样去反引号、转大写、忽略大小写匹配（`findField` 用 `equalsIgnoreCase`）。

**规则上下文变量清单**
`tableName, field, prodField, modelField, soaField, specField, fileSpec`

### 8.5 表名对照表（跨源实体对齐）

**问题场景**：生产库、DDM、SOA 往往由不同团队、不同时期建设，同一张表在三处的命名常常不一致（例如生产叫 `T_ACCT`、DDM 叫 `ACCT_MODEL`、SOA 叫 `ACCT_INFO`）。工具默认按"表名精确匹配"关联，若三处名字不同就会误报"文档有、库里没有"。这些老系统的表名通常无法改动（改了就失去"原样核对"的意义）。

**正解**：在工具侧维护一张 `表名对照表`，把三个来源的异名归一到同一个"逻辑表名"再比对——**三个老系统零改动**。

**配置方式**（`rules/table-mappings.yml`，与 `compare-rules.yml` 同目录，支持热加载）：
```yaml
mappings:
  - logicalName: ACCOUNT        # 你认的"逻辑表名"（结果中展示用）
    aliases:
      - T_ACCT                   # 生产库里的名字
      - ACCT_MODEL               # DDM 里的名字
      - ACCT_INFO                # SOA 里的名字
  - logicalName: CUSTOMER
    aliases: [T_CUST, CUSTOMER, CUST_INFO]
```
- **匹配规则**：不区分大小写、自动忽略反引号（`` ` ``）与前后空格。
- **未配置映射的表**：仍按原名精确匹配，老行为完全兼容。
- **热加载**：改完保存，约 60 秒（`app.table-mapping.hot-reload-seconds`）自动生效，无需重启。

**字段级映射（同表内异名字段对齐）**：在表映射下增加 `fields` 列表，把同表内不同来源的同一字段归一到逻辑字段名，避免 `客户名/CUST_NAME/custName` 这类异名被误报为"字段缺失"。详见 `CUSTOMER` 样例：
```yaml
  - logicalName: CUSTOMER
    aliases: [T_CUST, CUSTOMER, CUST_INFO]
    fields:
      - logicalName: CUST_NAME
        aliases: [客户名, custName]
```
比对时 `ComparePipeline.indexFieldsByLogical()` 先按 `TableMappingLoader.resolveFieldName(logicalTable, rawField)` 归一到逻辑字段名再逐字段评估；`中文字符检测` 规则已改为覆盖四来源、并精确归因到含中文的来源字段。

**在流水线的哪一步生效**：`ComparePipeline.indexByTable()` 建索引时、以及 `pickTableName()` 决定结果展示名时，都先调用 `TableMappingLoader.resolveLogicalName(rawTable)` 把原始表名转成逻辑名；字段级则在 `indexFieldsByLogical()` 内调用 `resolveFieldName()`。规则、解析器、比对逻辑均不感知映射，完全解耦。

**实现位置**：`config/TableMappingLoader.java`（加载 + 热加载 + 表/字段归一查询）、`config/MappingEntry.java` + `config/FieldMappingEntry.java`（YAML 结构）、`AppProperties.TableMapping`（配置项）。

**已验证**：样例内置了一组"完全异名"表——生产 `T_ACCT`、DDM `ACCT_MODEL`、SOA `ACCT_INFO`（且 DDM 故意少了 `CREATE_TIME` 字段）。触发比对后：
- 三者正确归并到逻辑表 `ACCOUNT` 关联比对；
- 仅命中 1 条真实问题：`模型缺失生产字段 | CREATE_TIME`（DDM 侧确实缺）；
- 未出现"T_ACCT 在 DDM 缺失"之类的误报；
- 原 `CUSTOMER`（中文名违规）、`AUDIT_LOG`（增量缺时间戳）等照常命中，未被影响。

---

## 9. 结果、工单与复核

### 9.1 结果表 `compare_result`
每条命中一条记录：`rule_name, category, severity, table_name, field_name, message, prod_value, model_value, trace_info`（`trace_info` 存结构化排查信息：条件、双侧字段、deliveryType、耗时、错误）。

### 9.2 复核工单 `review_ticket`
- 编号：`{prefix}-{yyyyMMdd}-{NNN}`（`prefix` 默认 `RT`，见 `app.ticket.no-prefix`）
- 状态流转：`PENDING → CONFIRMED / REJECTED / IGNORED`
- 字段：`ticket_no(唯一), task_id, severity, table_name, field_name, message, status, assignee, review_comment, notified, create_time, update_time`

### 9.3 复核流程
`PUT /api/tickets/{id}/review` 提交 `status / reviewer / comment`，状态落库；`GET /api/tickets?status=PENDING` 过滤；`GET /api/tickets/{id}` 看详情。

---

## 10. 通知（邮件）
- `MailService` 基于 Spring Mail + Freemarker
- **优雅降级**：当 `spring.mail.host` 为空（默认）时 `enabled=false`，只记日志不发信，比对照常进行
- 两类模板：`ticket-notify.ftl`（工单通知）、`run-summary.ftl`（运行汇总）

---

## 11. 定时调度（`CompareScheduler`）
- 每 `app.scheduler.poll-seconds`（默认 30 秒）轮询一次所有启用任务
- 用 Spring `CronExpression` 判定到期：
  - `lastRunTime == null`（首次）：**轮询即触发一次**
  - 否则用 `ce.next(lastRunTime)` 与 `now` 比较
- ⚠️ Spring `CronExpression` **没有 `previous()` 方法**（已踩坑并改为上述逻辑）
- 修改 cron 走 `PUT /api/config/tasks/{id}`，**无需重启**

---

## 12. 配置项（`application.yml` → `AppProperties`）

| 配置键 | 默认 | 说明 |
|---|---|---|
| `server.port` | 8080 | 服务端口 |
| `spring.datasource.url` | `jdbc:h2:mem:dqc;MODE=MySQL;...` | H2（MySQL 兼容） |
| `spring.h2.console.enabled` | true | H2 控制台 `/h2-console` |
| `spring.mail.host` | `""` | 空则邮件降级 |
| `app.input.base-dir` | `./data/input` | 输入根目录 |
| `app.rules.path` | `./rules/compare-rules.yml` | 规则文件 |
| `app.rules.hot-reload-seconds` | 60 | 热加载周期 |
| `app.scheduler.poll-seconds` | 30 | 调度轮询周期 |
| `app.ticket.no-prefix` | RT | 工单前缀 |
| `app.rule.timeout-ms` | 3000 | 单规则超时 |
| `app.table-mapping.path` | `./rules/table-mappings.yml` | 表名对照表文件 |
| `app.table-mapping.hot-reload-seconds` | 60 | 对照表热加载周期 |

**环境注意**：本运行环境注入了 `SERVER__PORT` 环境变量，Spring relaxed binding 会覆盖 `server.port`。启动需 `env -u SERVER__PORT java -jar ...`，否则会监听随机端口。

---

## 13. 数据库表结构（6 张，跨 H2/MySQL 兼容）
| 表 | 作用 |
|---|---|
| `compare_task_config` | 任务配置：cron、各源路径、收件人、last_run_time |
| `compare_task` | 执行记录：status、统计计数、起止时间、错误信息 |
| `compare_result` | 比对结果明细 |
| `review_ticket` | 复核工单（含 `uk_ticket_no`） |
| `operation_log` | 操作日志 |
| `app_lock` | 分布式运行锁（多实例部署保证同一任务单实例执行，带 TTL 自动恢复） |

> 种子数据：因 H2 不支持 `INSERT IGNORE`，默认配置由 `DataInitializer`（CommandLineRunner）在表空时注入，保证跨库兼容。
> 查询索引：`compare_result(task_id)`、`compare_task(task_config_id)`、`review_ticket(task_id / status)`，见 `db/schema.sql`。

---

## 14. REST API 一览

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/compare/trigger?configId=` | 手动触发；不传 configId 则触发所有启用任务 |
| GET | `/api/compare/tasks` | 任务执行列表（倒序） |
| GET | `/api/compare/tasks/{id}/results` | 某次任务的结果明细 |
| GET | `/api/compare/tasks/{id}/history-diff` | 与上一成功任务对比（added/persistent/resolved） |
| GET | `/api/tickets?status=PENDING` | 工单列表（可按状态筛选） |
| GET | `/api/tickets/{id}` | 工单详情 |
| PUT | `/api/tickets/{id}/review` | 提交复核（`status, reviewer, comment`） |
| PUT | `/api/config/tasks/{id}` | 改配置（cron 校验、路径、收件人、启用） |
| GET | `/` | 浏览器落地页（触发按钮 + 结果表） |
| GET | `/h2-console/` | H2 控制台（JDBC `jdbc:h2:mem:dqc`, 用户 `sa`, 空密码） |

**示例**
```bash
# 触发
curl -X POST http://localhost:8080/api/compare/trigger
# 看明细
curl http://localhost:8080/api/compare/tasks/1/results
# 复核工单
curl -X PUT http://localhost:8080/api/tickets/1/review \
  -H "Content-Type: application/json" \
  -d '{"status":"CONFIRMED","reviewer":"张三","comment":"确认违规"}'
# 改 cron（非法返回 400）
curl -X PUT http://localhost:8080/api/config/tasks/1 \
  -H "Content-Type: application/json" \
  -d '{"recipients":"dqc@example.com","cronExpression":"0 0 2 * * ?"}'
```

**history-diff 结构**：`HistoryDiff{ added[], persistent[], resolved[] }`，以 `ruleName|tableName|fieldName` 为键对比两次任务。

---

## 15. 启动流程

```bash
cd /Users/zingaltern/WorkBuddy/2026-07-27-21-43-25/metadata-compare

# 1) 打包（改了 Java 代码才需要；只改规则/样例数据不用重打包）
mvn -o -q package -DskipTests

# 2) 启动（unset SERVER__PORT 以使用配置里的 8080）
env -u SERVER__PORT java -jar target/metadata-compare.jar

# 3) 验证
curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/   # 期望 200
curl -X POST http://localhost:8080/api/compare/trigger          # 期望 total=6 critical=2

# 关闭
pkill -f metadata-compare.jar
```
**生产（MySQL）**：`java -jar target/metadata-compare.jar --spring.profiles.active=mysql`，连接串见 `application-mysql.yml` + `db/schema.sql`。

---

## 16. 成功判定标准（验收签名）

**成功 = 启动 200 + 登录鉴权生效 + 触发返回 `total=6 / critical=2 / warning=4 / ticket=2` + 两条严重项精准命中 + 日志无报错。**

> 注：启用 `CUSTOMER` 字段级映射（`客户名/CUST_NAME/custName → CUST_NAME`）后，原本因异名导致的「模型缺失生产字段→客户名」「生产缺失模型字段→CUST_NAME」两条虚假告警已消除，故 warning 由 6 降为 4。若删除该字段映射，warning 会变回 6。

样例数据下的标准命中：
- **CRITICAL（2）**
  - 中文字符检测 → `CUSTOMER.客户名`（样例里唯一中文名，只命中这一个；已覆盖四来源精确归因）
  - 增量文件缺少时间戳 → `AUDIT_LOG`（增量表无时间戳）
- **WARNING（4）**
  - 字段长度不一致 → `ORDER.ORDER_AMT`
  - 字段长度不一致 → `PRODUCT.PROD_NAME`
  - 必填字段在模型中变为可空 → `PRODUCT.PROD_STATUS`
  - 模型缺失生产字段 → `ACCOUNT.CREATE_TIME`（异名表 `T_ACCT`/`ACCT_MODEL`/`ACCT_INFO` 经对照表归一到 `ACCOUNT`，DDM 侧故意缺失该字段，属真实差异，无误报）

**反面信号（说明有问题）**
- `/` 返回 500
- `critical` 突然变成 19~20（中文字符规则在误报英文字段名）
- `critical` 只有 1，增量时间戳规则没命中（deliveryType 解析错乱）
- 日志仍在刷 `Skipped invalid entry` / `Exception`
- `critical>0` 但 `ticket=0`（工单没生成）

---

## 17. 已修复的关键缺陷（踩坑记录）

| # | 问题 | 根因 | 修复 |
|---|---|---|---|
| 1 | 中文字符检测误报所有英文字段 | QLExpress 吞掉 `\u` 反斜杠，正则退化为匹配 `[0-9A-Za-z]` | 改用 `hasChinese()` 函数（码点判断） |
| 2 | 端口变 52235 | 环境 `SERVER__PORT` 环境变量覆盖 `server.port` | 启动 `env -u SERVER__PORT` |
| 3 | 调度器首次运行永不触发 | `lastRunTime==null` 时用 `next(now-1s)` 得未来点 | 首次轮询立即触发，之后 `next(last)` |
| 4 | ConfigController 未校验 cron | 脏 cron 入库后调度器静默跳过 | 用 `CronExpression.parse` 预校验，非法返回 400 |
| 5 | 增量时间戳规则不命中 + 数据类型错位 | `ExcelSpecParser` 把「下发**类型**」误判为 `dataType`，覆盖 deliveryType 列 | deliveryType 判定优先级提到「类型」之前 |
| 6 | POI 解析 xlsx 刷告警 | Python 生成的 xlsx 共享字符串被 POI 严格解析器拒绝 | 改用 Apache POI 自身生成样例 xlsx |

---

## 18. 后续可扩展方向
- 更多数据源解析器（如 Kafka Schema、GraphQL、数据库直连 JDBC 反向采集）
- 规则模板市场 / 规则命中趋势看板
- 工单对接企业 IM（钉钉/企微）而非仅邮件
- 比对结果导出 Excel / 周期性报告
- 多租户 / 多环境配置
- Dockerfile + CI 流水线

---

## 19. 二期修复记录（2026-08-05）

| # | 问题 | 修复 |
|---|---|---|
| 1 | Swagger 2 YAML 解析失败被静默跳过，SOA 源实际不参与比对 | `SwaggerApiParser` 增加 V2 解析 + `SwaggerConverter` 转换回退；报告新增**源健康度**（各源文件数/实体数/失败文件），源为空或失败显式告警 |
| 2 | 下发方式校验缺失（SOA `x-deliveryType` 采集后未使用） | 新增规则「下发方式不一致」+ 自定义函数 `deliveryMismatch`，SOA 与文件规范两源交叉校验 |
| 3 | 规则消息/字段归因按规则名硬编码 | 规则新增 `message` 模板与 `fieldNameFromSource` 配置，消息与归因全部由 YAML 驱动 |
| 4 | 多实例部署并发不安全（进程内锁、工单号种子冲突） | 新增 `app_lock` 表 + 分布式锁（TTL 自动恢复）；工单号改为 UUID 临时号插入后按真实 id 生成，彻底消除跨实例碰撞 |
| 5 | 表级 last-wins 与字段级 first-wins 不一致 | 统一为首个出现者胜（`putIfAbsent`），重复逻辑表名/字段名打日志告警 |
| 6 | 解析器覆盖不足 | DDL 支持表级复合主键；Excel 支持英文表头与多 sheet；JSON 模型兼容 `entities/models/tables` 根键 |
| 7 | 测试覆盖不足（SOA bug 漏网） | 新增 Swagger/Excel/模型解析器测试与端到端集成测试（共 15 个，含验收签名、源健康度、下发方式规则） |
| 8 | QLExpress trace 控制台噪音 + 超时策略失真 | `ExpressRunner(true, false)` 关闭 trace；线程池改 `AbortPolicy`，池满时调用线程内联执行，避免中断请求线程 |
| 9 | 运维/安全项 | `compare_result(task_id)` 等查询索引；结果/工单分页（`page`/`size`，上限 500）；mysql profile 下默认口令直接拒绝启动；README 重写瘦身 |
